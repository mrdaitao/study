全排列问题（树遍历）
回溯算法
1.确定路径 记录你已经做过的选择
2.选择列表 可以选择的选项，表示你当前可以做出的选择
3.结束条件 选择列表为空的时候或者符合指定条件
回溯算法模板：
result = []
def backtrack(路径, 选择列表):
    if 满足结束条件:
        result.add(路径)
        return

    for 选择 in 选择列表:
        做选择
        backtrack(路径, 选择列表)
        撤销选择

其核心就是 for 循环里面的递归，在递归调用之前「做选择」，在递归调用之后「撤销选择」

写 backtrack 函数时，需要维护走过的「路径」和当前可以做的「选择列表」，当触发「结束条件」时，将「路径」记入结果集